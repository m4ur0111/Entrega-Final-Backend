<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reestablecer Contraseña</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: auto;
            text-align: center;
        }

        .logo {
            margin-bottom: 20px;
        }

        .greeting {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .message {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 10px;
        }

        .reset-form {
            margin-top: 20px;
        }

        .reset-input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .reset-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <p class="greeting">Hola {{ Nombre }}</p>
        <p class="message">Ingresa tu nueva contraseña para restablecerla:</p>
        
        <form class="reset-form" action="/procesar-contrasena" method="post" onsubmit="return validarContrasena()">
            <input type="hidden" name="token" value="{{ Token }}">
            <input class="reset-input" type="password" id="nueva-contrasena" name="nuevaContrasena" placeholder="Nueva Contraseña" required>
            <input class="reset-input" type="password" id="confirmar-contrasena" name="confirmarContrasena" placeholder="Confirmar Contraseña" required>
            <div id="mensaje-error" style="color: red;"></div>
            <button type="submit">Guardar Contraseña</button>
        </form>
    </div>
</body>
</html>
